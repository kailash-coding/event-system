# TODO

## Backend: Events + Login Setup

1. Confirm MongoDB connectivity (127.0.0.1:27017 reachable).  
2. Start backend server: `node src/server.js` (no `&&`).  
3. Update seed user credentials in `backend/src/seed.js` (set a known email + password).  
4. Run seed: `node src/seed.js`.  
5. Verify login works in frontend: test `/login` flow.  
6. Verify events API works: `GET http://127.0.0.1:5050/api/events/`.

